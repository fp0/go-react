# Todo App: Go + React + MongoDB

### .env file

```shell
MONGO_URI=<your_mongo_uri>
PORT=5000
ENV=development
```

### Compile and run

```shell
go run main.go
```

### Frontend

```shell
cd client
npm run dev
```